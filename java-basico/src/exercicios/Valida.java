/**
 * 
 */
package exercicios;

/**
 * Classe responsavel por armazenar os metodos de validações de dados.
 * @author Renato Duarte Sampaio
 *@since 25 de fev. de 2021
 */
public class Valida {
	public static boolean isIntPositivo ( int args) {
		return(args > 0 );
	}
	public static boolean isEmptyOrNull(Integer numero) {
		return (numero == null);
	
	}
	
}
